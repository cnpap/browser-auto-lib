{
  "name": "svelte-extension",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.app.json && pnpm -w exec tsc -p packages/svelte-extension/tsconfig.node.json"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "catalog:",
    "@tsconfig/svelte": "catalog:",
    "@types/node": "catalog:types",
    "svelte": "catalog:",
    "svelte-check": "catalog:",
    "typescript": "catalog:cli",
    "vite": "catalog:cli"
  }
}
